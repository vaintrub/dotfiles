model = "gpt-5.5"
model_reasoning_effort = "xhigh"
personality = "pragmatic"

[plugins."github@openai-curated"]
enabled = true

[plugins."google-drive@openai-curated"]
enabled = true

# Runtime-mutated sections ([projects.*], [notice], [tui.*], etc.) are
# stripped on `git add` by the clean filter in codex/scripts/. See the
# README "AI tooling" section for details.
#
# TODO: when https://github.com/openai/codex/issues/15433 lands (separate
# project trust state from config), drop the filter and simplify.
