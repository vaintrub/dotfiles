- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    ~/.vim:
    ~/.vimrc:
    ~/.zsh:
    ~/.zshrc:
    ~/.aliases:
    ~/.antigenrc:

- shell:
  - git submodule sync --recursive
  - git submodule update --init --recursive
