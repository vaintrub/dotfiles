# 1Password SSH agent — which keys it serves.
# Docs: https://www.1password.dev/ssh/agent/config
# Matched by item, not vault: a vault that doesn't match is silently ignored,
# so moving the key between vaults would empty the agent with no error.
[[ssh-keys]]
item = "Main SSH key"
