{{- /*
  Target paths (relative to $HOME) to delete on apply. Re-evaluated every run,
  so this is a short-lived migration step — drop the entry once every machine
  has applied. One path per line.
*/ -}}

